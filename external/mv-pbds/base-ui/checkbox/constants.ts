import { IconNames } from "@mvloans/base-ui.mv-svg-icon";

export const checkboxIcons = {
  default: IconNames.checkbox,
  disabled: IconNames.checkbox,
  checked: IconNames.checkboxSelected,
  indeterminate: IconNames.checkboxIntermediate,
};
