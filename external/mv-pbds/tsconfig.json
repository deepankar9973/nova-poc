{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "strict": false,
    "exactOptionalPropertyTypes": false,

    // Add any other specific compiler options needed for this sub-project here
    // For now, turning off strictness should be enough.
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}